% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MultiAssaySpatialExperiment-helpers.R
\name{prepMASE}
\alias{prepMASE}
\title{Prepare components for a MultiAssaySpatialExperiment}
\usage{
prepMASE(
  ExperimentList,
  colData,
  sampleMap,
  points = PointsLayerList(),
  shapes = ShapesLayerList(),
  images = RasterLayerList(),
  labels = RasterLayerList(),
  imgData = NULL,
  spatialMap = NULL,
  ...
)
}
\arguments{
\item{ExperimentList}{A \link[MultiAssayExperiment]{ExperimentList} or named
list of assays passed to \code{\link[MultiAssayExperiment]{prepMultiAssay}}.}

\item{colData}{A \linkS4class{DataFrame} of specimen-level metadata (one row
per \code{sampleMap$primary}), passed to \code{prepMultiAssay}.}

\item{sampleMap}{A \linkS4class{DataFrame} with MAE columns \code{assay},
\code{primary}, and \code{colname}, passed to \code{prepMultiAssay}.}

\item{points}{A \linkS4class{PointsLayerList} or named list of point layers.}

\item{shapes}{A \linkS4class{ShapesLayerList} or named list of shape layers.}

\item{images}{A \linkS4class{RasterLayerList} of image layers (default empty).}

\item{labels}{A \linkS4class{RasterLayerList} of label layers (default empty).}

\item{imgData}{Optional specimen-level image metadata \linkS4class{DataFrame}.}

\item{spatialMap}{Optional instance-level mapping \linkS4class{DataFrame}.}

\item{...}{Additional arguments passed to \code{prepMultiAssay} (\code{metadata},
\code{drops}).}
}
\value{
A \code{list} with elements \code{experiments}, \code{colData},
  \code{sampleMap}, \code{metadata}, \code{drops}, and spatial slot components.
}
\description{
Wraps \code{\link[MultiAssayExperiment]{prepMultiAssay}} and harmonizes spatial
slots. Returns a \code{list} suitable for
\code{\link{MultiAssaySpatialExperiment}} or \code{do.call}.
}
\examples{
mat <- matrix(1:6, 2, 3, dimnames = list(c("G1", "G2"), paste0("spot", 1:3)))
sm <- S4Vectors::DataFrame(
    assay = factor(rep("rna", 3), "rna"),
    primary = c("S1", "S1", "S2"),
    colname = paste0("spot", 1:3))
pts <- S4Vectors::DataFrame(
    x = 1:3, y = 1:3, instance_id = paste0("spot", 1:3))
prepared <- prepMASE(
    ExperimentList(rna = mat),
    S4Vectors::DataFrame(row.names = c("S1", "S2")),
    sm,
    points = PointsLayerList(tissue1 = pts),
    spatialMap = buildSpatialMap(sm, "tissue1", "points"))
do.call(MultiAssaySpatialExperiment, prepared)

}
\seealso{
\code{\link{buildSpatialMap}},
  \code{\link[MultiAssayExperiment]{prepMultiAssay}}
}
