% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MultiAssaySpatialExperiment-read-generics.R
\name{readGeoParquetForMASE}
\alias{readGeoParquetForMASE}
\alias{readGeoParquetForMASE,character-method}
\title{Read GeoParquet Files for MASE}
\usage{
readGeoParquetForMASE(file_path, ...)

\S4method{readGeoParquetForMASE}{character}(file_path, ...)
}
\arguments{
\item{file_path}{Character path to GeoParquet file}

\item{...}{Additional arguments passed to format-specific methods}
}
\value{
sf object or equivalent object
}
\description{
Read a GeoParquet file via \pkg{sfarrow} for geometry layers in MASE readers.
}
\examples{
if (requireNamespace("sfarrow", quietly = TRUE) &&
    requireNamespace("sf", quietly = TRUE)) {
    tmp <- tempfile(fileext = ".parquet")
    pt <- sf::st_sfc(sf::st_point(c(1, 2)))
    sf_obj <- sf::st_sf(id = 1L, geometry = pt)
    sfarrow::st_write_parquet(sf_obj, tmp)
    readGeoParquetForMASE(tmp)
    unlink(tmp)
}

}
